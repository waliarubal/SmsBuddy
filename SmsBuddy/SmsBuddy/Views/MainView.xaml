<Window
    x:Class="SmsBuddy.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SmsBuddy.ViewModels"
    Title="SMS Buddy"
    Width="800"
    Height="600"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ToolBarTray Orientation="Vertical">
            <ToolBarTray.Resources>
                <Style x:Key="NavigationButtonStyle" TargetType="Button">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="Command" Value="{Binding Path=ShowViewCommand, Mode=OneTime}" />
                </Style>
            </ToolBarTray.Resources>
            <ToolBar ToolBarTray.IsLocked="True">
                <Button CommandParameter="{x:Type vm:SmsViewModel}" Style="{StaticResource ResourceKey=NavigationButtonStyle}">Simple SMS</Button>
                <Button CommandParameter="{x:Type vm:GatewayViewModel}" Style="{StaticResource ResourceKey=NavigationButtonStyle}">Gateway</Button>
            </ToolBar>
        </ToolBarTray>
        <ContentPresenter
            Grid.Column="1"
            Margin="4"
            Content="{Binding Path=ChildView, Mode=OneWay}" />
    </Grid>
</Window>
